(ns vinzi.cdm.globals
  (:use [clojure core pprint])
  )



;; NOT USED anymore
;(defn getHtmlResponse "Wrap body as a text/html message." [body]   
;  {:status  200
;   :headers {"Content-Type" "text/html"}
;   :body    body})
;
;(defn getTextResponse "Wrap body as a text/plain message." [body]   
;  {:status  200
;   :headers {"Content-Type" "text/plain"}
;   :body    body})
;
;
;(def reqCnt (atom 0))
    
